grid background -> Blog page(code)
hero page vector image ->  top-left = Emergency fund, Risk profilling
                           top -right = Asset allocation 
                           bottom -right = yearly rebalancing


font -bricolage(heading main)
quicksand, merriweather

Book a call -> /contact(Book free consultation call)
call now -> direct call 
contact Us -> /contact
we receive your message after the user submit your contact form


contact form ->
Method 1: save data in db and send email to the team with the message
Method 2 : use sheetDb.io(currently using)
Method 3 :
-> Build a small dashboard (React + Node.js API) for finance:
-> List all contacts/bookings.
-> Include “Reply” button that sends email directly.
-> Optional: export to CSV if needed.
-> Optionally, mirror the data to Google Sheets for reporting/sharing.


User Form (Website)
        |
        | 1️⃣ User submits form
        v
Backend API (Node.js/Express)
        |
        | 2️⃣ Save submission to MongoDB
        |
        | 3️⃣ Emit WebSocket event (new submission)
        v
MongoDB (Database)
        ^
        | Optional: store for persistence
        |
Dashboard (Finance Team)
        |
        | 4️⃣ WebSocket receives event
        v
UI Updates in Real-Time (new row appears)
        |
        | 5️⃣ Finance team can click "Reply"
        v
Backend API sends Email (NodeMailer/Resend/SendGrid)
        |
        | 6️⃣ Update MongoDB with "replied" status
        v
Dashboard UI updates automatically


integrate calendy  in (Book a free consultation Call)
Add Blog page to CMS
